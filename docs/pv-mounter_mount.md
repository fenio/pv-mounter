## pv-mounter mount

Mount a PersistentVolumeClaim (PVC) to a local directory.

### Synopsis

Mount a PersistentVolumeClaim (PVC) to a local directory.

This command sets up necessary Kubernetes resources and establishes a connection
to mount the specified PVC locally. Use --backend to select the mount method
(ssh for SSHFS or nfs for NFS-Ganesha).

```
pv-mounter mount [flags] <namespace> <pvc-name> <local-mount-point>
```

### Options

```
      --backend string        Mount backend: 'ssh' (default) or 'nfs'
      --cpu-limit string      Set CPU limit for the volume-exposer container (optional)
      --debug                 Enable debug mode to print additional information (default: false)
  -h, --help                  help for mount
      --image string          Custom container image for the volume-exposer (optional)
      --image-secret string   Kubernetes secret name for accessing private registry (optional)
      --needs-root            Mount the filesystem using the root account (default: false)
```

### SEE ALSO

* [pv-mounter](pv-mounter.md)	 - Mount and unmount Kubernetes PersistentVolumes using SSHFS or NFS

###### Auto generated by spf13/cobra on 17-Feb-2026
